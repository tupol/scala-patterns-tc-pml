package shapes.pimpmylib

import shapes.untouchable.{Circle => UCircle, Square => USquare}

/**
 *  This is a sample of Pimp My Lib Pattern; for more details see
 *  @link{http://www.artima.com/weblogs/viewpost.jsp?thread=179766}
 */
object Main extends App {
//  val shapes = List(UCircle(2), USquare(2))
//  val areas = shapes.map(_.area)
//  println(areas)
}
